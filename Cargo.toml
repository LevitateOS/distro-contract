[package]
name = "distro-contract"
version = "0.1.0"
edition = "2021"
description = "Trait contracts for LevitateOS distro family — zero-cost interfaces between crates"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1.0"
